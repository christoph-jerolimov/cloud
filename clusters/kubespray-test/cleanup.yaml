- name: DELETE cluster kubespray-test
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: DELETE server kubespray-test
      hetzner.hcloud.server:
        name: kubespray-test
        server_type: cpx32
        location: nbg1
        image: centos-stream-10
        state: absent
